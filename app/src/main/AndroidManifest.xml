<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eventhorizonwebdesign.cpocketreference">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ASCIIListActivity" />
        <activity android:name=".PrimitiveListActivity" />
        <activity android:name=".TutorialActivity" />
        <activity android:name=".ReferenceListActivity" />
        <activity android:name=".KeywordListActivity"></activity>
    </application>

</manifest>